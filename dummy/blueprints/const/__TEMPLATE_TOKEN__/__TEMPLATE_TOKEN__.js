import React from 'react'
import PropTypes from 'prop-types'
import styles from './<% TEMPLATE_TOKEN pascalCase %>.sass'

const <% TEMPLATE_TOKEN pascalCase %> = (props) => {
  return (
    <div></div>
  )
}
<% TEMPLATE_TOKEN pascalCase %>.displayName = '<% TEMPLATE_TOKEN pascalCase %>'
<% TEMPLATE_TOKEN pascalCase %>.defaultProps = {
}
<% TEMPLATE_TOKEN pascalCase %>.propTypes = {
  /** Prop description */
  propname: PropTypes.type
}
export default <% TEMPLATE_TOKEN pascalCase %>
